services:
  web:
    image: corentinth/enclosed:latest
    user: 1000:1000
    restart: on-failure
    stop_grace_period: 1m
    volumes:
    - ${APP_DATA_DIR}/data:/app/.data
    x-runtipi:
      internal_port: 8787
      is_main: true
x-runtipi:
  schema_version: 2
