services:
  app:
    image: linuxserver/sqlitebrowser:latest
    restart: on-failure
    volumes:
    - ${APP_DATA_DIR}/data/config:/config
    environment:
    - PUID=1000
    - PGID=1000
    - TZ=Etc/CET
    x-runtipi:
      internal_port: 3000
      is_main: true
x-runtipi:
  schema_version: 2
