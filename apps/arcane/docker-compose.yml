services:
  docker:
    image: docker:27.2.0-dind@sha256:f9f72ad901a78f27be922b2d320bbc263174f12919c1b37e6a01f828fa904565
    privileged: true
    network_mode: host
    stop_grace_period: 1m
    restart: unless-stopped
    environment:
      DOCKER_ENSURE_BRIDGE: dind0:10.32.0.1/16
    entrypoint: /entrypoint.sh
    command: "dockerd\n  --bridge dind0\n  --data-root /data/data\n  --exec-root /data/exec\n  --host unix:///data/docker.sock\n  --pidfile /data/docker.pid\n"
    volumes:
    - ${APP_DATA_DIR}/entrypoint.sh:/entrypoint.sh
    - ${APP_DATA_DIR}/data/docker:/data
    labels:
    - runtipi.managed=true
  arcane:
    image: ghcr.io/getarcaneapp/arcane:v2.8.0@sha256:9146935e0797ce14a340616ceab2f77b3bc61d1b10befb0718a4be7f01485e2c
    depends_on:
    - docker
    restart: unless-stopped
    environment:
      APP_URL: http://localhost:3552
      PUID: 1000
      PGID: 1000
      ENCRYPTION_KEY: ${APP_PASSWORD}
      JWT_SECRET: ${APP_PASSWORD}
    volumes:
    - ${APP_DATA_DIR}/data/app_data:/app/data
    - ${APP_DATA_DIR}/data/projects:/app/data/projects
    - ${APP_DATA_DIR}/data/docker:/var/run
    labels:
    - runtipi.managed=true
    x-runtipi:
      internal_port: 3552
      is_main: true
x-runtipi:
  schema_version: 2
