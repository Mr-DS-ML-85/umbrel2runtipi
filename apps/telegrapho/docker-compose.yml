services:
  app:
    image: ghcr.io/egzola/telegrapho:1.0.1
    restart: on-failure
    stop_grace_period: 1m
    environment:
      PORT: 3712
    x-runtipi:
      internal_port: 3712
      is_main: true
x-runtipi:
  schema_version: 2
