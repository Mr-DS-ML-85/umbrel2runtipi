services:
  app:
    image: ghcr.io/besoeasy/zeronote:latest
    user: 1000:1000
    restart: on-failure
    stop_grace_period: 5m
    environment:
    - NODE_ENV=production
    x-runtipi:
      internal_port: 8000
      is_main: true
x-runtipi:
  schema_version: 2
