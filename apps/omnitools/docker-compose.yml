services:
  app:
    image: iib0011/omni-tools:latest
    security_opt:
    - no-new-privileges:true
    restart: unless-stopped
    labels:
    - runtipi.managed=true
    x-runtipi:
      internal_port: 80
      is_main: true
x-runtipi:
  schema_version: 2
