services:
  web:
    image: voidic/snort:latest
    init: true
    labels:
    - runtipi.managed=true
    restart: unless-stopped
    x-runtipi:
      internal_port: 8080
      is_main: true
x-runtipi:
  schema_version: 2
