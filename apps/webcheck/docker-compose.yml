services:
  server:
    image: ghcr.io/lissy93/web-check:latest
    restart: on-failure
    volumes:
    - ${APP_DATA_DIR}/data:/app/data
    x-runtipi:
      internal_port: 3000
      is_main: true
x-runtipi:
  schema_version: 2
