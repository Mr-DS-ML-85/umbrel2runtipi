services:
  server:
    image: ghcr.io/matiasdesuu/thinkdashboard:latest
    user: 1000:1000
    restart: on-failure
    volumes:
    - ${APP_DATA_DIR}/data:/app/data
    environment:
    - PORT=8080
    x-runtipi:
      internal_port: 8080
      is_main: true
x-runtipi:
  schema_version: 2
